apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.team }}-{{ .Values.environment }}
  labels: {{ toYamlPretty .Values.labels | nindent 4}}
  name: {{ .Chart.Name }}-service
spec:
{{- with .Values.service }}
  type: {{ .type }}
  ports:
    - port: {{ .port }}
      protocol: {{ .protocol }}
      targetPort: {{ .targetPort }}
{{- end }}
  selector: {{ toYamlPretty .Values.labels | nindent 4 }}